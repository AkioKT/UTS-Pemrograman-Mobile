{/* <View
            style={{
              position: "absolute",
              ...(index === 0
                ? { top: 50, right: 0 }
                : { bottom: 50, left: -10 }),
                backgroundColor: "green"
            }}
          >
            <Text
              style={{
                color: "#fff",
                fontFamily: "Pixel-Bold",
                fontSize: 20,
                position: "absolute",
                ...(index === 0
                  ? { top: 0, right: 85 }
                  : { bottom: 190, left: 75 }),
              }}
            >
              {item.name}
            </Text>
            <Image
              source={character[item.avatar]}
              style={{
                width: 200,
                height: 200,
              }}
              resizeMode="contain"
            />
            <Text
              style={{
                color: "#fff",
                fontFamily: "Pixel-Bold",
                fontSize: 20,
                position: "absolute",
                ...(index === 0
                  ? { top: 180, right: 55 }
                  : { bottom: -10, left: 60 }),
              }}
            >
              (score: {item.score || 0})
            </Text>
          </View> */}